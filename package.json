{
  "name": "ezzi-world-root",
  "private": true,
  "version": "1.0.0",

  "engines": {
    "node": ">=20 <21"
  },

  "packageManager": "npm@10.2.4",

  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev": "npm --prefix apps/web run dev",
    "start": "npm --prefix apps/web run start"
  },

  "devDependencies": {
    "next": "14.2.12"
  }
}

